"use client";
import dynamic from "next/dynamic";import{useCallback,useEffect,useState}from"react";import{getLatestReadings,getTanks}from"@/services/api";import type{SensorReading,Tank}from"@/components/dashboard/types";import{ModuleError,ModuleLoading,ModuleScaffold}from"./ModuleScaffold";import{useApiSession}from"./useApiSession";
const TankMap=dynamic(()=>import("@/components/dashboard/TankMap"),{ssr:false,loading:()=> <div className="h-[600px] animate-pulse rounded-2xl bg-slate-200"/>});
export default function MapPageClient(){const session=useApiSession();const[tanks,setTanks]=useState<Tank[]>([]);const[readings,setReadings]=useState<SensorReading[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);try{const[t,r]=await Promise.all([getTanks(),getLatestReadings()]);setTanks(t);setReadings(r)}catch{setError("GIS data could not be loaded from the monitoring API.")}finally{setLoading(false)}},[]);useEffect(()=>{const id=window.setTimeout(()=>{if(session)void load();else if(session===false)setLoading(false)},0);return()=>window.clearTimeout(id)},[session,load]);return <ModuleScaffold eyebrow="Geospatial operations" title="Tank GIS Map" description="OpenStreetMap view of every monitored asset, coloured by its latest sewage and gas condition.">{loading?<ModuleLoading/>:error?<ModuleError message={error} retry={()=>void load()}/>:tanks.length?<TankMap tanks={tanks} readings={readings} route={null}/>:<div className="rounded-2xl bg-white p-12 text-center text-slate-500">No mapped tanks are registered.</div>}</ModuleScaffold>}
