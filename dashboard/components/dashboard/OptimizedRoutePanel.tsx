import type { OptimizedRoute } from "./types";

export default function OptimizedRoutePanel({ route }: { route: OptimizedRoute | null }) {
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><h2 className="text-lg font-semibold text-slate-900">Optimized truck route</h2><p className="mt-1 text-sm text-slate-500">Nearest-stop route for open maintenance work.</p></div>{route && <p className="text-right text-sm font-bold text-cyan-700">{route.totalDistanceKm.toFixed(1)} km<br/><span className="text-xs font-medium text-slate-500">{route.stops.length} stops</span></p>}</div>{!route || route.stops.length === 0 ? <p className="mt-6 text-sm text-slate-500">No open maintenance stops require routing.</p> : <ol className="mt-5 space-y-3">{route.stops.map((stop) => <li key={stop.tankId} className="flex gap-3 rounded-xl bg-slate-50 p-3"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-cyan-700 text-xs font-bold text-white">{stop.sequence}</span><div className="min-w-0"><p className="font-semibold text-slate-900">{stop.tankName}</p><p className="truncate text-xs text-slate-500">{stop.location} · {stop.distanceFromPreviousKm.toFixed(1)} km from previous</p><p className="mt-1 text-xs font-medium text-slate-700">{stop.task}</p></div></li>)}</ol>}</section>;
}
