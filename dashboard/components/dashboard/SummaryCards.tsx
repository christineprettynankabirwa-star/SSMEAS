interface SummaryCardsProps { totalTanks: number; onlineTanks: number; activeAlerts: number; averageFillLevel: number | null; }
export default function SummaryCards(props: SummaryCardsProps) {
  const items = [{ label: "Total tanks", value: props.totalTanks.toString(), accent: "bg-cyan-600", tint: "bg-cyan-50" }, { label: "Online tanks", value: props.onlineTanks.toString(), accent: "bg-emerald-500", tint: "bg-emerald-50" }, { label: "Active alerts", value: props.activeAlerts.toString(), accent: "bg-amber-500", tint: "bg-amber-50" }, { label: "Average tank fill", value: props.averageFillLevel === null ? "No reading" : `${props.averageFillLevel.toFixed(1)}%`, accent: "bg-cyan-700", tint: "bg-sky-50" }];
  return <section aria-label="System summary" className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map((item) => <article key={item.label} className="relative overflow-hidden rounded-2xl border border-slate-200/80 bg-white p-5 shadow-[0_6px_20px_rgba(15,23,42,0.05)]"><span className={`absolute left-5 top-0 h-1 w-12 rounded-b-full ${item.accent}`} /><div className={`mb-4 grid size-9 place-items-center rounded-xl ${item.tint}`}><span className={`size-2.5 rounded-full ${item.accent}`} /></div><p className="text-sm font-medium text-slate-500">{item.label}</p><p className="mt-1.5 text-3xl font-bold tracking-tight text-slate-950">{item.value}</p></article>)}</section>;
}
