import type { AlertItem } from "./types";
const styles: Record<AlertItem["severity"], string> = { critical: "border-red-200 bg-red-50 text-red-800", warning: "border-amber-200 bg-amber-50 text-amber-800", info: "border-blue-200 bg-blue-50 text-blue-800" };
export default function AlertsPanel({ alerts }: { alerts: AlertItem[] }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold text-slate-900">Active alerts</h2><span className="text-sm font-medium text-slate-500">{alerts.length} open</span></div><div className="mt-4 space-y-3">{alerts.length === 0 ? <p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">No active alerts.</p> : alerts.map((alert) => <article key={alert.id} className={`rounded-xl border p-3 ${styles[alert.severity]}`}><p className="text-sm font-semibold">{alert.alert_type} · {alert.tank_name}</p><p className="mt-1 text-sm opacity-90">{alert.message}</p><time className="mt-2 block text-xs opacity-75">{new Date(alert.created_at).toLocaleString()}</time></article>)}</div></section>; }
