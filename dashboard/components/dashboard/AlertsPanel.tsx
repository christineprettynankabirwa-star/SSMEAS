import Link from "next/link";
import Card from "@/components/ui/Card";
import type { AlertItem } from "./types";
const styles: Record<AlertItem["severity"], string> = { critical: "border-red-200 bg-red-50 text-red-800", warning: "border-amber-200 bg-amber-50 text-amber-800", info: "border-blue-200 bg-blue-50 text-blue-800" };
export default function AlertsPanel({ alerts }: { alerts: AlertItem[] }) { return <Card as="section" className="h-full min-h-64 p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold text-slate-900">Priority alerts</h2><p className="mt-1 text-sm text-slate-500">Issues requiring operational review.</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{alerts.length} open</span></div><div className="mt-4 space-y-3">{alerts.length === 0 ? <p className="rounded-xl bg-emerald-50 p-4 text-sm font-medium text-emerald-700">All monitored conditions are within configured limits.</p> : alerts.map((alert) => <Link href={`/tanks/${encodeURIComponent(alert.tank_id)}`} key={alert.id} className={`ui-interactive-card block rounded-xl border p-3 ${styles[alert.severity]}`}><div className="flex items-start justify-between gap-3"><p className="text-sm font-semibold">{alert.alert_type} · {alert.tank_name}</p><span className="shrink-0 text-xs font-bold">View →</span></div><p className="mt-1 text-sm opacity-90">{alert.message}</p><time className="mt-2 block text-xs opacity-75">{new Date(alert.created_at).toLocaleString()}</time></Link>)}</div></Card>; }
