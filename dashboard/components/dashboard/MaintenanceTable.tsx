import Link from "next/link";
import type { MaintenanceItem } from "./types";
export default function MaintenanceTable({ items }: { items: MaintenanceItem[] }) { return <section className="min-h-64 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="p-5"><h2 className="text-lg font-semibold text-slate-900">Maintenance schedule</h2><p className="mt-1 text-sm text-slate-500">Planned field operations.</p></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-semibold">Tank</th><th className="px-5 py-3 font-semibold">Task</th><th className="px-5 py-3 font-semibold">Scheduled</th><th className="px-5 py-3 font-semibold">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{items.length === 0 ? <tr><td colSpan={4} className="px-5 py-8 text-center text-slate-500">No maintenance is scheduled.</td></tr> : items.map((item) => <tr key={item.id} className="transition-colors hover:bg-cyan-50/40"><td className="whitespace-nowrap px-5 py-4"><Link href={`/tanks/${encodeURIComponent(item.tank_id)}`} className="font-semibold text-cyan-800 hover:underline">{item.tank_name}</Link></td><td className="px-5 py-4 text-slate-600">{item.task}</td><td className="whitespace-nowrap px-5 py-4 text-slate-600">{new Date(item.scheduled_for).toLocaleString()}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-700">{item.status.replaceAll("_", " ")}</span></td></tr>)}</tbody></table></div></section>; }
