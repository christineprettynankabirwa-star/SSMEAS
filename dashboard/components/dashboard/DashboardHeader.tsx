import { useEffect, useState } from "react";
const formatTime = (date: Date) => new Intl.DateTimeFormat("en-GB", { hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false }).format(date);
export default function DashboardHeader({ lastUpdated }: { lastUpdated: Date | null }) {
  const [now, setNow] = useState<Date | null>(null);
  useEffect(() => { const tick = () => setNow(new Date()); tick(); const id = window.setInterval(tick, 1_000); return () => window.clearInterval(id); }, []);
  return <header className="flex flex-col gap-4 border-b border-slate-200/80 pb-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><span className="live-chip"><span />Live operations</span><span className="text-xs font-semibold text-slate-400">Kampala network</span></div><h1 className="mt-2 text-2xl font-bold tracking-tight text-slate-950 sm:text-[1.7rem]">Environmental Operations Overview</h1><p className="mt-1 text-sm text-slate-500">Real-time sewage infrastructure intelligence and field coordination</p></div><div className="flex items-center gap-3 sm:text-right"><div><p className="font-mono text-lg font-bold tracking-wide text-slate-800">{now ? formatTime(now) : "--:--:--"}</p><p className="text-[11px] font-medium text-slate-400">Updated {lastUpdated ? formatTime(lastUpdated) : "awaiting data"}</p></div><button type="button" aria-label="Notifications" className="relative grid size-10 place-items-center rounded-xl border border-slate-200 bg-white text-slate-600 shadow-sm transition hover:border-cyan-300 hover:text-cyan-700"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" className="size-5"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4" /></svg><span className="absolute right-2 top-2 size-2 rounded-full bg-rose-500 ring-2 ring-white" /></button></div></header>;
}
